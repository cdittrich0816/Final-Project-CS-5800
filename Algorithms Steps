steps. 

1. prepare data, pre processing, parse csv. -- coordinate, label

after parsing, visualize all the labels.(A B C) 

2. calculate the distance of the node to be classified to any other nodes-- 

visualize the node to be classified.

3. sort the distance, then pick up top k smallest k nodes.

visualize step-by-step distance calculation 
Node -> a
Node -> b
Node -> c

4. compare the classification of the top k nodes, then classify it into the highest percent classification.
